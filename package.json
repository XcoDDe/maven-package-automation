{
  "name": "maven-tasks-json",
  "displayName": "Maven Tasks JSON",
  "description": "Automatically creates tasks.json file with Maven build task configuration",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.79.0"
  },
  "publisher": "Thompson Oretan",
  "categories": [
    "Other"
  ],
 
  "activationEvents": [
    "onStartupFinished"
  ],

  "main": "./extension.js",
  
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js",
    "vscode:prepublish": "npm run build",
    "package":"vsce package",
    "build":"npm run build"
  },
  "devDependencies": {
    "@types/vscode": "^1.79.0",
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "20.2.5",
    "eslint": "^8.41.0",
    "glob": "^8.1.0",
    "mocha": "^10.2.0",
    "typescript": "^5.1.3",
    "@vscode/test-electron": "^2.3.2"
  }
}
